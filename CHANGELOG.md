# Журнал изменений FSM PRO

## ✅ Сессия 2026-08-21 (4) — профиль мастера и умные статусы
- **Чужие задачи — только просмотр**: в карточке чужой задачи кнопок действий нет, плашка «Задача другого мастера». «Я на объекте» — только для своих.
- **Умные кнопки по статусу**: свободная → «Взяться за задачу»; своя новая → «Начать работу»; в работе → «Продолжить работу»; на проверке → серая «ждём руководителя»; завершённая → зелёная плашка.
- **«Выполнена» → «Завершена»**: финальный статус после принятия руководителем переименован везде (бейджи, тосты, типы).
- **Профиль мастера**: кружок-аватар в шапке → фото (со сжатием) или цветная аватарка с инициалами, возраст, «о себе». Заполняет сам мастер; имя/телефон/специализации — только руководитель. Руководитель видит профиль в карточке «Сотрудники».
- Коммит: `cec7488`

## ✅ Сессия 2026-08-21 (3) — 5 фаз по отзывам владельца
- **Витрина «Все задачи»**: своих задач больше нет (только в «Мои»); свободные — зелёная подсветка + метка «Новая», чужие — серым блоком ниже.
- **Сотрудники (было «Бригады»)**: «Заблокировать» (код не пускает: «Доступ закрыт»), «Уволить» (подтверждение, код стирается, задачи освобождаются), «Уволить всех» (чистый лист).
- **Новый работник**: пустые «Мои задачи» + приветствие «Вы новый работник — возьмите зелёную задачу».
- **Яндекс.Карты**: фейковая карта убрана; кнопка «Найти на Яндекс.Картах» по геометке (обновляется при действиях мастера).
- **Свои типы работ**: универсальный список (Доставка, Логистика, Помощь и уход, Уборка…) + «Своё…» с запоминанием.
- **Демо** обновлено под новый поток; **синхронизация вкладок** одного браузера (BroadcastChannel) — задачи/люди видны сразу в двух окнах.
- Коммит: `a779a82`

## ✅ Сессия 2026-08-21 (2b) — реструктуризация и номер сборки
- **Номер сборки** на экране входа, в «Помощи» и настройках админки (дата+время сборки, генерируется автоматически).
- **Реструктуризация `app/`**: исходники переехали в `app/`, корень репо — только собранный сайт. Причина: старые деплои заливали собранный сайт в корень и затирали исходный `index.html` — из-за этого vite собирал старую версию (это была причина «на сайте всё старое»).
- **SW v3**: автообновление приложения без чистки кэша (сеть-first для страниц, авто-перезагрузка при активации новой версии).
- Коммиты: `2709702`, `45a5cc3`

## ✅ Сессия 2026-08-21 (2) — Этапы 1–4: «ноль текста», доработки, админка

### Этап 1 — исправления
- **Фото**: два способа добавления — «Сделать фото» (камера) и «Из галереи» (выбор с телефона); сжатие через canvas (макс. 1280px, JPEG 80%) — фото больше не перегружают IndexedDB (`src/utils/image.ts`).
- **«На доработке»**: при возврате отчёта у мастера на карточке задачи и на экранах детали/выполнения — красный баннер с причиной от диспетчера. Повторная отправка на проверку снимает флаг (`task.needsRework`).
- **Чек-лист крупнее**: каждый пункт — большая кнопка высотой от 52px с чекбоксом 28px, тапается вся строка.

### Этап 2 — «ноль текста»
- **Быстрые кнопки-комментарии**: 6 типовых ситуаций чипсами («Требуются материалы», «Нет доступа на объект» и т.д.) — без набора текста.
- **«Я на объекте»**: одна кнопка в карточке задачи — фиксирует GPS-координаты и время прибытия (`task.arrivalAt` + `arrivalLocation`).
- Голосовые сообщения — пока НЕ делали (по решению владельца).

### Этап 3 — личный чат
- Чат мастера: переключатель «Общий чат» / «Лично с диспетчером».
- Чат руководителя: адресат — общий чат или конкретный мастер (пилюли с аватарами).
- Сообщения с `recipientId` видят только двое; чат теперь сохраняется в localStorage и переживает перезагрузку.

### Этап 4 — администрирование
- **Коды доступа настраиваются в админке** (Настройки → Коды доступа): коды работника/руководителя + сброс к 1111/9999. В коде приложения кодов больше нет (`auth.ts` читает localStorage).
- **Личные коды мастеров**: при добавлении сотрудника можно выдать личный код — вход по нему открывает панель именно этого мастера. Проверка на дубли.
- **Добавление сотрудников** в разделе «Бригады» (ФИО, телефон, специализации, личный код).
- **Аватарки**: если фото нет — цветной круг с инициалами (`src/components/shared/Avatar.tsx`, стабильный цвет по имени).
- **Кнопки-причины возврата** в модалке проверки: 5 типовых причин чипсами + своя причина.

### Не тронуто (по решению владельца)
- База Supabase не подключается — вся проверка локально, как раньше.
- Push-уведомления — проработаны варианты (Telegram-бот / Web Push / in-app), реализация после этапа Supabase.

### Сборка
- `tsc -b` — 0 ошибок; `vite build` — успешно.

## ✅ Что сделано (ранее)

### Фаза 1 — Порядок во входе
- Вход только по коду доступа, с чётким разделением ролей:
  - **`1111`** → панель работника (мастер)
  - **`9999`** → панель руководителя
- Неверный код → ошибка «Неверный код», никуда не пускает (раньше любой код открывал панель работника).
- Убран хак с переходом через `?view=admin` — роли разведены на уровне приложения.

### Фаза 2 — Разделение ролей и экранов
- Отдельная **панель работника** (мобильная) и отдельная **панель руководителя**.
- В шапке обеих панелей — кнопка «Выйти» для быстрого переключения.

### Фаза 3 — Демо-режим отдельно
- Кнопка **«Демо-режим (обучение)»** вынесена отдельно от входа по коду.
- В демо — жёлтая плашка «Учебный демо-режим», обучающий экран и плавающий **гид** (лампочка) с пошаговой инструкцией.
- Обычный вход работника больше не показывает «учебных» надписей.

### Фаза 4 — Локальная проверка
- Сборка `tsc + vite build` без ошибок, линтер — 0 ошибок.

### Панель работника (мастер)
- **4 вкладки**: «Все задачи», «Мои», «Чат», «Помощь».
- **«Все задачи» (витрина)**: свои задачи отдельно, чужие и свободные — в выделенной пунктирной рамке. У свободных — кнопка **«Взяться за задачу»**.
- **Чат с руководителем** — отдельная вкладка, с бейджем непрочитанных.
- **Фиксация времени** при «Начать работу»: дата и время старта записываются, показывается плашка «Начало работы зафиксировано».
- **Чек-лист**: виден в деталях задачи и на экране выполнения; можно отмечать пункты, **добавлять свои** («+») и удалять.
- **Фото**: добавление + **удаление** (крестик).
- Переключатель **«Онлайн/Офлайн»** в шапке — видно руководителю.

### Панель руководителя
- **Бригады**: реальный статус онлайн/офлайн каждой бригады; KPI «Активных бригад» считает именно онлайн.
- **Настройки** (рабочая модалка): звук уведомлений (сигнал при сообщении от работника), показ/скрытие офлайн-бригад.
- Вместо абстрактного «Сегодня» — реальная дата и время.
- Убраны неактивные поля «Создание задачи» и календарь.
- Чат с рабочими (сообщения сохраняются, видны с обеих сторон).

### Связь «руководитель ↔ работник» (реального времени, без сервера)
- **Чат** синхронизируется между ролями и между вкладками через localStorage + событие `storage` — сообщение, отправленное работником, мгновенно видно руководителю (и наоборот), даже в другой вкладке браузера (имитация двух устройств).
- **Статус «онлайн/офлайн»** работника сохраняется и передаётся руководителю в реальном времени (в разделе «Бригады» и в KPI «Активных бригад»).
- **Непрочитанные сообщения** считаются отдельно для каждой стороны: у работника бейдж на вкладке «Чат», у руководителя — счётчик; при открытии чата сбрасывается.
- Звуковой сигнал у руководителя при новом сообщении от работника (если включён в настройках).

### Техническое
- Service worker регистрируется **только в production** (в dev не мешает обновлениям).
- Новый модуль `src/auth.ts` — единая точка логики кодов доступа.

---

## 🔜 Планы на будущее

### Ближайшие (без базы данных)
- **Геолокация** — фиксация координат при начале/завершении работы, отметка на карте.
- **Карта бригад** в реальном времени у руководителя (сейчас — статичная заглушка).
- **Таймлайн задачи** — наглядная лента статусов и событий по каждой заявке.
- **Подпись заказчика** на экране завершения (canvas) + акт выполненных работ (PDF уже частично есть).
- **Автоназначение задач** по специализации и загрузке мастера.

### Среднесрочные
- **База данных / сервер** (PostgreSQL — схема уже в `database_schema.sql`):
  - синхронизация IndexedDB → сервер (очередь offline уже заложена в `db.ts`),
  - реальный многопользовательский режим, история изменений.
- **Роли и права** на сервере, авторизация по коду/паролю.
- **Уведомления** (push) о новых задачах и сообщениях.

### На основе похожих решений (для ориентира)
Западные FSM-сервисы: **ServiceM8, Jobber, Housecall Pro**. Российские: **Planado, Mobiledoo, «Мобильный прораб»**. Полезные паттерны для заимствования:
- геотрекинг бригад и маршрутизация выездов;
- фотоотчёты с геометкой и временем;
- автоформирование актов/смет;
- статусная лента и таймлайн заказа;
- расписание/календарь выездов (вернуть позже, но в виде планировщика).

---

## 🚀 Деплой
```bash
npm run build       # сборка в dist/
python deploy_gh.py # выгрузка dist/ на GitHub Pages (нужен GITHUB_TOKEN в .env)
```

Страница: https://unicompact-stack.github.io/fsm-pro/
